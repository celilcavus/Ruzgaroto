@model IEnumerable<About>

<table class="table table-hover">
    <thead>
        <tr>
            <td>ID</td>
            <td>ANA BAŞLIK</td>
            <td>BAŞLIK</td>
            <td>AÇIKLAMA</td>
            <td>AKTİF</td>
            <td>GÜNCELLE</td>
            <td>SİL</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.BaseTitle</td>
                <td>@item.Title</td>
                <td>@item.Description</td>
                @if (item.IsActive)
                {
                    <td>Aktif</td>
                }
                else
                {
                    <td>Aktif Değil</td>
                }
                <td><a asp-action="Update" asp-controller="About" asp-route-Id="@item.Id" class="btn btn-warning">Güncelle</a></td>
                <td><a asp-action="Delete" asp-controller="About" asp-route-Id="@item.Id" class="btn btn-danger">Sil</a></td>
            </tr>
        }
    </tbody>
</table>